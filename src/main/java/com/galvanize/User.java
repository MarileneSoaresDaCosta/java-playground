package com.galvanize;

// enums exercise: Create an enum named Role for the User class with the values OWNER, ADMIN, and USER.
public class User {
    private static enum Role {
        OWNER, ADMIN, USER
    }

}
